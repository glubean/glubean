# DummyJSON test credentials (used by auth exercises)
# Copy this file to .env.secrets and fill in the values:
#   cp .env.secrets.example .env.secrets
#
# These are DummyJSON's built-in test accounts (public, safe to use).
USERNAME=emilys
PASSWORD=emilyspass
