# Glubean API Test Environment
#
# Copy this file to .env and fill in the values.
#
# BASE_URL: The URL of your local glubean server
BASE_URL=http://localhost:3002

# API_KEY: Your personal API key for authentication.
#
# How to get an API key:
# 1. Start the glubean server locally
# 2. Login via browser at http://localhost:3000
# 3. Go to Settings > API Keys
# 4. Create a new API key
# 5. Copy the key (it's only shown once!)
#
# Store it in .env.secrets (not .env) for security:
#   echo "API_KEY=glubean_..." >> .env.secrets
