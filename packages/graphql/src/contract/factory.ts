/**
 * GraphQL contract factory.
 *
 * Placeholder for CG-12 (factory + registration). Filled after adapter
 * lands.
 *
 * Responsibility: implement `createGraphqlFactory` / `createGraphqlRoot`
 * providing `contract.graphql.with("name", defaults)(id, spec)` UX.
 *
 * Reference: `packages/grpc/src/contract/factory.ts` (shipped) — GraphQL
 * version will mirror the scoped-defaults pattern with GraphQL-specific
 * default fields (types, endpoint, default headers).
 */

// TODO(CG-12): factory + registration.

export {};
