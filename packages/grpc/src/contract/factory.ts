/**
 * gRPC contract factory.
 *
 * Placeholder for CG-4 (factory + registration). Filled in working step
 * after adapter lands.
 *
 * Responsibility: implement `createGrpcFactory` / `createGrpcRoot`
 * providing `contract.grpc.with("name", defaults)(id, spec)` UX.
 *
 * Reference: `packages/sdk/src/contract-http/factory.ts`.
 */

// TODO(CG-4): factory + registration.

export {};
