/**
 * gRPC contract types.
 *
 * Placeholder for CG-2 (types design). Filled in next working step.
 *
 * Responsibility: define `GrpcContractSpec`, `GrpcCase`,
 * `GrpcPayloadSchemas<Req, Res>`, `GrpcContractMeta`,
 * `GrpcFlowCaseOutput`. Mirrors structure of
 * `packages/sdk/src/contract-http/types.ts` but with gRPC semantics
 * (service/method target, metadata, status code, unary for Phase 1).
 */

// TODO(CG-2): design and implement types here.

export {};
